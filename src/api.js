import {Routes, Route, BrowserRouter } from "react-router-dom";

return {
  <BrowserRouter>
    <Routes>
      <Route path="/"element={<HomeViewContainer/>} />
    </Routes>
  </BrowserRouter>  
  

}